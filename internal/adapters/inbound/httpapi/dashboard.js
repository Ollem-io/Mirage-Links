/* Embedded, deliberately small HTMX-compatible fragment driver. */
(()=>{const run=async(el,method,url,body)=>{const r=await fetch(url,{method,body,credentials:"same-origin",headers:{"HX-Request":"true","X-Mirage-CSRF":(document.cookie.match(/(?:^|; )mirage_dashboard_csrf=([^;]+)/)||[])[1]||""}});const redirect=r.headers.get("HX-Redirect");if(redirect){location.assign(redirect);return}const target=document.querySelector(el.getAttribute("hx-target"));if(target&&r.ok)target.innerHTML=await r.text();else if(target)target.innerHTML='<p class="error" role="alert">Request failed.</p>'};document.addEventListener("submit",e=>{const f=e.target,verb=["hx-post","hx-delete"].find(x=>f.hasAttribute(x));if(!verb)return;e.preventDefault();run(f,verb.slice(3).toUpperCase(),f.getAttribute(verb),new FormData(f))});document.addEventListener("click",e=>{const b=e.target.closest("[hx-get]");if(!b)return;e.preventDefault();run(b,"GET",b.getAttribute("hx-get"))});addEventListener("DOMContentLoaded",()=>document.querySelectorAll('[hx-trigger="load"]').forEach(x=>run(x,"GET",x.getAttribute("hx-get"))))})();
