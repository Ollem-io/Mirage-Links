# Admin token

See [admin-token.md](admin-token.md). Configure `admin.token_hash_file`, initialize it with explicit token and hash paths, and use admin credentials only for installation-wide space/link operations. Space tokens remain restricted to their own space.
